import { makeStyles } from '@mui/styles'

export const useStyles = makeStyles({
  genresWrapper: {
    width: '50%',
    margin: '20px',
  },
  genreTitle: {
    textTransform: 'uppercase',
    fontSize: '1.2rem',
    fontWeight: 'bold',
    marginBottom: '10px',
  },
})
