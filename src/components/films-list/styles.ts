import { makeStyles } from '@mui/styles'

export const useStyles = makeStyles({
  gridItem: {
    display: 'flex',
    justifyContent: 'center',
  },
  parentContainer: {
    marginTop: '40px',
    marginBottom: '120px',
  },
})
