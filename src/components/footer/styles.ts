import { makeStyles } from '@mui/styles'

export const useStyles = makeStyles({
  footerWrap: {
    position: 'fixed',
    left: 0,
    bottom: 0,
  },
  toolBar: {
    display: 'flex',
    justifyContent: 'flex-end',
  },
})
